services:
  - type: web
    name: be3-ai-transformer
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: EMBEDDING_MODE
        value: cloud
      - key: HF_TOKEN
        sync: false # Must be provided in Render Dashboard
      - key: EMBEDDING_MODEL_KEY
        value: bge-small
      - key: CLIP_SPACE_URL
        value: https://behug3-be3-image.hf.space
      - key: PORT
        value: 10000
